<UserControl x:Class="MyHRMobile.FhirGatewayTool.Views.DisplayTextEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyHRMobile.FhirGatewayTool.Views"
             xmlns:CustomControl="clr-namespace:MyHRMobile.FhirGatewayTool.CustomControl"
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

  <CustomControl:GridControl x:Name="GridMain" 
                     ShowCustomGridLines="True" 
                     GridLineBrush="DarkGreen"  
                     GridLineThickness="2" 
                     Margin="0">

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <avalonedit:TextEditor Name="TextEditorBody" Grid.Column="0" Grid.Row="0">
    </avalonedit:TextEditor>

  </CustomControl:GridControl>
</UserControl>
